<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extend Sign Template to include Masked Field type -->
    <record id="sign_template_view_form_inherit_masked" model="ir.ui.view">
        <field name="name">sign.template.form.inherit.masked</field>
        <field name="model">sign.template</field>
        <field name="inherit_id" ref="sign.sign_template_view_form"/>
        <field name="arch" type="xml">
            <!-- The field_type selection is automatically extended by the Python model -->
            <!-- No additional XML changes needed for the selection field itself -->
        </field>
    </record>
</odoo>
